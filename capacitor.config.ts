import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jotoalert.app',
  appName: 'jotoalert',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
